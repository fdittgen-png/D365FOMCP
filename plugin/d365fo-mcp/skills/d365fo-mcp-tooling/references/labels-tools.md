# d365labels — D365FO Labels — tool reference

_Generated by `npm run gen:plugin-refs` from `src/azure/labels-tools.js`. Do not edit by hand._

MCP server key: `d365labels` · 4 tools · all read-only (`readOnlyHint`), idempotent, closed-world.

Every label in every shipped language, always with its developer description (the " ;" comment that says what the label is for). `labels_lookup` for ids (any casing, with or without @), `labels_search` for a UI string, `labels_where_used` for the objects and properties that carry it (needs the XRef snapshot), `labels_for_object` for the labels one object carries.

| Tool | One-line purpose |
|---|---|
| `labels_lookup` | Label ids → text in every stored language (or the languages given) plus the developer description, file, module and origin. |
| `labels_search` | Reverse lookup: label TEXT → label ids, across every stored language (full-text, prefix match per word). |
| `labels_where_used` | Where a label is used (XRef graph): object, element and PROPERTY (Label, HelpText, Caption, DeveloperDocumentation, …, or Code for X++ uses), with counts per pr |
| `labels_for_object` | The labels an object carries (XRef graph): element, property, label id and text per language (default en-US) with the description. |

## `labels_lookup`

Label ids → text in every stored language (or the languages given) plus the developer description, file, module and origin. Accepts @SYS154828, SYS154828, @File:Key. One id is a batch of one.

| Param | Type | Required | Description |
|---|---|---|---|
| `label_ids` | array<string (min 2, max 200)> | yes |  |
| `languages` | array<string (min 2, max 10)> | no | Omit for every stored language. |
| `format` | `markdown` \| `toon` \| `auto` | default `"auto"` | auto (default) = smallest; markdown to quote verbatim. |

## `labels_search`

Reverse lookup: label TEXT → label ids, across every stored language (full-text, prefix match per word). Narrow with language, label_file, modules or origin; page with cursor.

| Param | Type | Required | Description |
|---|---|---|---|
| `text` | string (min 2, max 200) | yes |  |
| `language` | string (min 2, max 10) | no |  |
| `label_file` | string (min 1, max 100) | no |  |
| `modules` | array<string (min 1, max 200)> | no | Optional: limit results to these modules/models (case-insensitive), e.g. ["iExtension"] or ["ApplicationSuite","ApplicationPlatform"]. Use the service's list-modules/stats tool to see the scanned modules and their build versions. |
| `origin` | `microsoft` \| `isv` \| `custom` | no |  |
| `limit` | integer (≥1, ≤100) | default `20` |  |
| `cursor` | string (max 500) | no | Page cursor: the `next_cursor` of the previous response. |
| `format` | `markdown` \| `toon` \| `auto` | default `"auto"` | auto (default) = smallest; markdown to quote verbatim. |

## `labels_where_used`

Where a label is used (XRef graph): object, element and PROPERTY (Label, HelpText, Caption, DeveloperDocumentation, …, or Code for X++ uses), with counts per property and the label text + description. Filter by property or object_type; page with cursor.

| Param | Type | Required | Description |
|---|---|---|---|
| `label_id` | string (min 2, max 200) | yes |  |
| `property` | string (min 1, max 60) | no | e.g. Label, HelpText, Caption, Code |
| `object_type` | string (min 1, max 40) | no | XRef type token or friendly name: table, form, enum, class, entity, menu_item, privilege, duty, edt |
| `limit` | integer (≥1, ≤200) | default `50` |  |
| `cursor` | string (max 500) | no | Page cursor: the `next_cursor` of the previous response. |
| `format` | `markdown` \| `toon` \| `auto` | default `"auto"` | auto (default) = smallest; markdown to quote verbatim. |

## `labels_for_object`

The labels an object carries (XRef graph): element, property, label id and text per language (default en-US) with the description. object_type: table, form, enum, class, entity, menu_item, privilege, duty, edt, or an XRef type token.

| Param | Type | Required | Description |
|---|---|---|---|
| `object_type` | string (min 1, max 40) | yes |  |
| `object_name` | string (min 1, max 200) | yes |  |
| `languages` | array<string (min 2, max 10)> | default `["en-US"]` |  |
| `properties` | array<string (min 1, max 60)> | no |  |
| `limit` | integer (≥1, ≤500) | default `200` |  |
| `format` | `markdown` \| `toon` \| `auto` | default `"auto"` | auto (default) = smallest; markdown to quote verbatim. |

